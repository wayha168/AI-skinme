# Copy to .env and fill in. Do not commit .env (keep credentials secret).

# Optional: Spring backend URL for saving chat logs and feedback
# SPRING_BACKEND_URL=http://localhost:8080

# Optional: MySQL (skinme_db) for "check with database" in chat and product search
# Set these to use product recommendations from your live database.
# MYSQL_HOST=your-mysql-host
# MYSQL_PORT=3306
# MYSQL_USER=your_user
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=skinme_db
# MYSQL_PRODUCTS_TABLE=product

# Optional: OpenAI for LLM replies
# OPENAI_API_KEY=sk-...
